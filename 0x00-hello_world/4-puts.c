#include <stdio.h>
/**
 * main - A program when run  prints a line with puts function
 * Return: 0 (succeeded)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
