#include <stdio.h>
/**
 * main - Empty point
 * Description: 'Print alphabets lowercase'
 * return: Always 0
 */
int main(void)
{
	int m = 97;

	while (m <= 122)
	{
		putchar(m);
		m++;
	}
	putchar('\n');
	return (0);
}
